# EAS Build ignore file - exclude large/unnecessary files from upload
# Keep workspace structure intact for pnpm install resolution
node_modules/
.git/
.turbo/
.next/
.env.local
.env*.local
# Include android/ and ios/ from local prebuild so EAS uses correct autolinking
# (pnpm strict mode causes remote prebuild to resolve packages incorrectly)
