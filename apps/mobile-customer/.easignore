# EAS Build ignore file - exclude large/unnecessary files from upload
# Keep workspace structure intact for pnpm install resolution
node_modules/
.git/
.turbo/
.next/
.env.local
.env*.local
# Let EAS run expo prebuild (CNG mode) so autolinking handles all hoisted packages
android/
ios/
